% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairs-maker.R
\name{convert_to_list}
\alias{convert_to_list}
\title{Convert pairings tibble to list}
\usage{
convert_to_list(data_frame)
}
\arguments{
\item{data_frame}{A tibble with columns \code{week}, \code{group}, and \code{pairing}: the output of \code{convert_to_df()}.}
}
\value{
A list of named character vectors with class \code{pairings}, matching the structure expected by \code{create_pairs()}.
}
\description{
A helper function to convert a pairings tibble (created by \code{convert_to_df()}) back into a list format compatible with \code{create_pairs()}.
The tibble must contain the columns \code{week}, \code{group}, and \code{pairing}, as produced by \code{convert_to_df()}.
}
\examples{
# Create a class list
roster <- LETTERS[1:10]

# Generate pairs
pairings <- create_pairs(roster)

# Convert to tibble
data_frame <- convert_to_df(pairings)

# Convert back to list format
roster_list <- convert_to_list(data_frame)

}
